{% extends 'base.html.twig' %}

{% block title %}Option{% endblock %}

{% block body %}
    <h1>Option</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ option.id }}</td>
            </tr>
            <tr>
                <th>Libele</th>
                <td>{{ option.libele }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ option.description }}</td>
            </tr>
            <tr>
                <th>Tarification</th>
                <td>{{ option.tarification }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('option_index') }}">back to list</a>

    <a href="{{ path('option_edit', {'id': option.id}) }}">edit</a>

    {{ include('option/_delete_form.html.twig') }}
{% endblock %}
